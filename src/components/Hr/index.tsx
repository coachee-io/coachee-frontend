import styled from 'styled-components'

const Hr = styled.hr`
  width: 80%;
  margin: 0 auto;
  border: 0.5px solid rgba(0, 0, 0, 0.2);
  margin: 16px 0 32px;
  &:last-child {
    display: none;
  }
`

export default Hr
