import styled from 'styled-components'

const Hr = styled.hr`
  width: 80%;
  border: 0.5px solid rgba(0, 0, 0, 0.2);
  margin: 16px auto 32px auto;
  &:last-child {
    display: none;
  }
`

export default Hr
