import React, { PureComponent } from "react"

// AuthContainer should be connected to the redux store
// It should be aware of the authentication state of the user and render the correct routes
// Non Auth pages: Homepage, Signup, Login, Become a coach
// Auth pages: Coach and any nested routes belonging to it

class AuthContainer extends PureComponent {
  render() {
    return <div>bla</div>
  }
}

export default AuthContainer